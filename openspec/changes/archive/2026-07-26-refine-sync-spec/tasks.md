## 1. Validación de la spec

- [x] 1.1 Ejecutar `openspec validate refine-sync-spec --strict` y corregir errores de formato
- [x] 1.2 Confirmar que cada requirement MODIFIED conserva el bloque completo original más los cambios (no solo el fragmento editado)

## 2. Cierre del change

- [ ] 2.1 Archivar el change `refine-sync-spec` con `openspec archive` para actualizar `openspec/specs/sync/spec.md`
